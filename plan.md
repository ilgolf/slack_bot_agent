# piplup-agent-v2 실행 흐름 개선 계획

## 목표

`piplup-agent-v2`는 Slack 스레드에서 로컬 `~/orca/projects`의 프로젝트를 분석하는
봇이다. 개선 목표는 모델이 필요한 최소 파일만 읽고, 근거 있는 답변을 반환하도록
제어하는 것이다. 대상이 없거나 모호하면 추측·전수 탐색 대신 사용자에게 대상을 묻는다.

실제 Slack·LLM API는 테스트에서 호출하지 않는다. 모든 단계는 실패하는 테스트를 먼저
작성하고, 최소 변경으로 통과시킨 뒤 Ruff, mypy, pytest를 실행한다.

## 범위

- Slack 멘션과 이전 스레드 맥락을 함께 처리
- 로컬 프로젝트 안에서의 읽기 전용 파일 도구
- LLM의 다음 행동 결정과 결과 기반 최종 답변
- 도구 호출·실패·종료의 안전한 운영 로그

다음은 범위 밖이다: GitHub URL clone/원격 분석, 파일 쓰기, 셸 명령 실행, 모든
프로젝트의 기본 전수 분석, LLM 내부 추론 전문의 노출 또는 저장.

## 핵심 원칙

1. 명시된 프로젝트명 또는 스레드에서 확정된 프로젝트명만 사용한다.
2. LLM은 한 번에 최종 답변 또는 다음 도구 호출 하나만 선택한다.
3. 컨트롤러가 프로젝트·경로·도구·호출 예산을 검증한 뒤에만 실행한다.
4. README 요약은 README만 읽고 즉시 종료한다.
5. 도구로 읽은 결과만 근거로 답하고, 근거가 없으면 그 사실을 답한다.
6. 로그에는 안전한 실행 요약만 남기고 파일 본문·비밀값·숨은 추론은 남기지 않는다.

## 목표 흐름

```text
Slack app_mention
  → “분석 중입니다…” 응답
  → 이전 ThreadContextStore 맥락 + 현재 요청
  → 대상 프로젝트와 요청 종류 결정
  → LLM: final 또는 next_tool_call 하나 제안
  → PolicyGuard: 도구·프로젝트·경로·예산 검증
  → read_file / list_files / list_projects 실행
  → 도구 결과를 LLM에 전달
  → 충분하면 구조화된 최종 결과 검증
  → Slack 스레드 답변 + 완료 상태 저장
```

Socket Mode가 기본 전송 방식이다. `SLACK_BOT_TOKEN`과 `SLACK_APP_TOKEN`을 사용하며,
ngrok·Request URL은 필요하지 않다.

## 요청별 기대 동작

| 요청 | 허용 도구 호출 | 종료 조건 |
| --- | --- | --- |
| `foo README.md 요약해줘` | `read_file(foo, "README.md")` 한 번 | README 기반 요약 |
| 같은 스레드의 `README.md 요약해줘` | 이전 맥락에서 `foo` 식별 후 README 한 번 읽기 | README 기반 요약 |
| `foo의 인증 흐름 분석` | 관련 디렉터리·텍스트 파일을 제한적으로 탐색 | 근거·한계 포함 분석 |
| `README.md 요약해줘` (대상 없음) | 없음 | 프로젝트명 요청 |
| GitHub URL만 제공 | 없음 | 로컬 프로젝트명 요청 또는 없음 안내 |

## 현재 기반

- [x] 별도 Slack 앱, Socket Mode, 스레드 응답
- [x] 환경 기반 Slack·LLM 설정과 OpenAI/Anthropic 선택
- [x] 프로젝트 경계를 검증하는 `list_projects`, `list_files`, `read_file`
- [x] 스레드 맥락 저장·읽기
- [x] 도구 호출·실패·Socket Mode 순환 파일 로그
- [x] fenced JSON, non-breaking space, 바이너리 파일의 안전한 처리
- [x] README 요청의 좁은 탐색 지침과 스레드 맥락 전달

## 구현 단계

### 1. 요청과 대상 프로젝트를 명시적으로 모델링

- [x] `AnalysisRequest`를 도입해 현재 요청, 이전 맥락, 대상 프로젝트, 요청 종류를
      분리한다.
- [x] 현재 요청의 프로젝트명이 이전 맥락보다 우선하도록 한다.
- [x] 대상이 없으면 LLM·파일 도구 없이 프로젝트명을 요청한다.
- [x] GitHub URL은 이름 후보로만 파싱하고, 로컬에 없으면 clone하지 않는다.

검증: 새 스레드, 후속 스레드, 대상 변경, 없는 프로젝트, URL만 주어진 경우.

### 2. 좁은 요청의 결정적 빠른 경로

- [x] `README.md` 요약과 특정 파일 요약을 분류한다.
- [x] README 요약은 `read_file(project, "README.md")` 한 번만 허용한다.
- [x] 특정 파일 요약은 지정 파일 하나만 읽도록 한다.
- [x] 빠른 경로가 실패하면 안전한 오류 또는 대상 확인 요청으로 끝낸다.

검증: README 요청이 `list_projects`, `list_files`, 다른 프로젝트·파일을 호출하지 않는지
테스트한다.

### 3. 넓은 분석용 단일-행동 제어 루프

- [x] LLM 응답을 `final` 또는 `tool_call`의 구조화된 결정으로 파싱한다.
- [x] 한 LLM 턴에는 도구 호출 하나만 허용한다.
- [x] `PolicyGuard`가 도구명, 프로젝트명, 상대 경로, 요청별 호출 예산을 검증한다.
- [x] 동일 도구·인자의 반복을 감지해 재호출하지 않고 종료한다.
- [x] 전역 한도 도달 시에는 실행한 도구와 부족한 정보를 설명하는 실패 결과를 반환한다.

검증: 반복 호출, 알 수 없는 도구, 경로 이탈, 예산 초과, 도구 실패와 정상 종료.

### 4. 근거 있는 최종 답변

- [x] `summary`, `findings`, `sources`, `limitations`를 최종 결과 스키마로 정의한다.
- [x] `sources`에는 실제로 읽은 프로젝트 상대 경로만 포함한다.
- [x] 근거 파일 없이 생성된 분석은 거부하고 대상·추가 정보를 요청한다.
- [x] Slack에는 요약, 핵심 발견, 근거 파일, 한계를 렌더링한다.

검증: 근거 없는 요약 거부, README 출처 표기, 유효하지 않은 JSON과 스키마 오류.

### 5. 스레드 상태와 중복 이벤트

- [x] 스레드별 상태를 `idle → planning → running → finalizing → completed/failed`로
      관리한다.
- [x] 동일 Slack 이벤트의 중복 분석을 막는 idempotency key를 적용한다.
- [x] 진행 메시지는 최소화하고 완료·실패 응답은 한 번만 맥락에 저장한다.

검증: 동일 이벤트 재전송, 빠른 연속 멘션, 분석 중 후속 요청.

### 6. 관측성과 운영 정리

- [x] 모든 로그에 요청 ID와 Slack 채널·스레드 ID를 포함한다.
- [x] `plan_selected`, `tool_call_started`, `tool_call_completed`,
      `tool_call_failed`, `analysis_completed`, `analysis_failed` 이벤트를 고정한다.
- [x] 파일 본문·토큰·내부 추론이 로그에 없는지 테스트한다.
- [x] 처리하지 않는 Slack 이벤트를 구독 해제하거나 의도적으로 acknowledge해
      `Unhandled request` 경고를 없앤다.
- [x] 로그 회전·보관·장애 확인 절차를 README에 문서화한다.

## 수용 기준

- 같은 스레드에서 프로젝트를 한 번 지정한 후 `README.md 요약해줘`라고 보내면 해당
  README만 읽고 답한다.
- 첫 요청에 대상이 없으면 전체 프로젝트를 탐색하지 않고 프로젝트명을 묻는다.
- 답변은 실제로 읽은 파일을 출처로 표시하며, 읽지 않은 코드·정책을 사실처럼 말하지
  않는다.
- 모든 도구 호출은 정책 가드와 예산을 통과한 뒤에만 실행된다.
- 도구 시작·종료·실패를 로그에서 추적할 수 있다.
- 잘못된 LLM 출력, 바이너리 파일, 경로 이탈, 반복 호출, 호출 한도가 Slack 프로세스를
  중단시키지 않는다.
- `uv run ruff check src tests`, `uv run mypy`, `uv run pytest`가 통과한다.

## 권장 구현 순서

1. 대상 프로젝트와 요청 종류의 결정적 분류
2. README/단일 파일 빠른 경로
3. 넓은 분석의 단일-행동 제어 루프
4. 근거·한계가 있는 최종 결과 스키마
5. 스레드 상태·중복 이벤트·구조화된 로그
6. 실제 Slack에서 README 요약, 모호한 요청, 코드 흐름 분석, 없는 프로젝트를 수동 검증

---

# Phase 2: AGENTS.md·Skill 기반 코드 에이전트

## 목표

현재의 읽기 전용 분석·파일 생성 봇을 확장해, 프로젝트의 `AGENTS.md`와 신뢰된 Skill을
참조하며 계획, 코드 변경, 검증까지 수행하는 Slack 코드 에이전트로 만든다.

LLM은 실행 계획과 다음 행동을 제안하고, 애플리케이션은 프로젝트 경계·도구 권한·사용자
확인을 검증한 뒤에만 실제 작업을 실행한다.

## 목표 흐름

```text
Slack 요청
  → 대상 프로젝트·작업 의도 분류
  → ProjectContextLoader
      ├─ AGENTS.md 계층 탐색
      ├─ 프로젝트 구조·Git 상태 수집
      └─ 신뢰된 Skill 선택
  → 구조화된 실행 계획
  → Slack 미리보기 및 `실행` 확인
  → 제한된 도구 실행
      ├─ 읽기 / 쓰기
      ├─ 테스트 / 린트 / 타입 검사
      └─ Git diff 확인
  → 검증 결과·변경 파일·위험을 Slack에 보고
```

## 지시 우선순위

```text
시스템·운영 정책
  > 봇의 고정 안전 정책
  > 상위 프로젝트 AGENTS.md
  > 대상 디렉터리 AGENTS.md
  > 허용된 Skill 지침
  > Slack 사용자 요청
```

프로젝트 안의 `AGENTS.md`와 Skill은 코드 규칙·테스트 방식·파일 배치를 안내할 수 있지만,
시스템 정책을 바꾸거나 임의의 외부 명령·비밀값 노출·권한 상승을 지시할 수 없다.

## 구성 요소

| 구성 요소 | 책임 |
| --- | --- |
| `RequestRouter` | 분석, 리뷰, 문서 생성, 코드 변경, 테스트, 파일 생성을 분류 |
| `ProjectContextLoader` | 프로젝트 루트, 계층형 `AGENTS.md`, Git 상태, 대상 파일 수집 |
| `SkillRegistry` | allowlist 안의 Skill만 선택·로드하고 적용 근거 기록 |
| `Planner` | 목표, 영향 파일, 단계, 검증 명령, 위험도를 구조화 |
| `PolicyGuard` | 읽기/쓰기/명령 범위, 위험 명령, 사용자 확인 여부 검증 |
| `Executor` | 한 단계씩 도구 실행 및 결과 기록 |
| `Verifier` | 테스트, 린트, 타입 검사, diff, 미해결 위험 검증 |
| `AuditLog` | 계획, 도구 호출, 파일 변경, 검증 결과를 비밀값 없이 기록 |

## 실행 권한 등급

| 등급 | 작업 | 실행 방식 |
| --- | --- | --- |
| 읽기 전용 | 분석, README 요약, 코드 리뷰, AGENTS/Skill 로드 | 자동 실행 |
| 가역적 쓰기 | 새 문서·CSV·XLSX·새 코드 파일 | 미리보기 후 `저장`/`실행` |
| 기존 파일 변경 | 리팩터링, 버그 수정, 설정 변경 | 계획·영향 파일·검증 명령 확인 후 `실행` |
| 고위험 | 의존성 변경, Git push, 외부 네트워크, 삭제 | 별도 명시 승인 및 제한된 정책 |

## 구현 체크리스트

### 7. 프로젝트 컨텍스트와 AGENTS.md

- [x] 프로젝트 루트부터 대상 디렉터리까지의 `AGENTS.md`를 순서대로 탐색한다.
- [x] 컨텍스트 로더가 파일별 출처·우선순위·적용 여부를 구조화해 반환한다.
- [x] 프로젝트 밖의 `AGENTS.md` 또는 심볼릭 링크 경로 이탈을 차단한다.
- [x] Git 상태와 변경 파일 목록을 읽기 전용 컨텍스트로 추가한다.

검증: 중첩된 `AGENTS.md`, 충돌 규칙, 경로 이탈, 파일 부재, Git 미초기화 프로젝트.

### 8. 신뢰된 Skill registry

- [x] 신뢰된 전역 Skill 루트와 프로젝트별 allowlist를 설정한다.
- [x] 요청 의도에 맞는 최소 Skill만 선택한다.
- [x] Skill의 `SKILL.md`를 완전히 읽고 적용된 이름·버전을 감사 로그에 남긴다.
- [x] 프로젝트 안의 임의 `SKILL.md`는 allowlist 없이 실행 지침으로 사용하지 않는다.

검증: 코드 리뷰, 문서 생성, 스프레드시트 생성, 미등록 Skill, 상충하는 Skill.

### 9. 계획·확인·실행 프로토콜

- [x] `ExecutionPlan`에 목표, 영향 파일, 단계, 검증 명령, 위험도, 적용 AGENTS/Skill을
      포함한다.
- [x] 기존 파일 수정과 명령 실행은 Slack `실행` 확인 전에는 수행하지 않는다.
- [x] `취소`는 보류된 계획만 제거하고 파일 변경을 만들지 않는다.
- [x] 실행 중 중복 Slack 이벤트는 기존 실행 상태를 재사용한다.

검증: 계획 미리보기, 실행 확인, 취소, 중복 이벤트, 만료된 보류 계획.

### 10. 제한된 코드 도구

- [x] `read_file`, `list_files`, `grep`은 프로젝트 경계 안에서만 실행한다.
- [x] `write_file`은 계획에 포함된 허용 경로만 생성·수정하고 기존 파일은 백업 또는 diff를
      제공한다.
- [x] `run_tests`, `run_lint`, `run_typecheck`은 allowlist 명령만 실행한다.
- [x] 셸 메타문자, 네트워크 명령, 광범위 삭제, Git push는 기본 차단한다.

검증: 허용 경로, 경로 이탈, 명령 삽입, 실패한 테스트, 기존 파일 변경 diff.

### 11. 검증과 결과 보고

- [x] 실행 후 변경 파일, diff 요약, 테스트·린트·타입 검사 결과를 수집한다.
- [x] 검증 실패 시 성공으로 보고하지 않고 실패 원인과 다음 선택지를 응답한다.
- [x] Slack 최종 응답에 변경 내용, 적용된 AGENTS/Skill, 검증 결과, 미검증 위험을
      포함한다.
- [x] 감사 로그는 비밀값·파일 본문·LLM 내부 추론을 기록하지 않는다.

## Phase 2 수용 기준

- 코드 변경 요청은 먼저 적용 규칙과 실행 계획을 Slack에 보여준다.
- `실행` 전에는 기존 프로젝트 파일이나 명령 실행 상태를 변경하지 않는다.
- 실행은 선택한 프로젝트 내부와 allowlist 명령으로 한정된다.
- 최종 응답은 변경 파일, 검증 결과, 적용한 AGENTS/Skill, 남은 위험을 포함한다.
- 프로젝트 지침 또는 Skill이 안전 정책과 충돌하면 안전 정책이 우선한다.

---

# Phase 3: PC별 Linear GraphQL 직접 연동

## 목표 흐름

```text
Slack Linear 요청
  → LinearIntegrationWorkflow (프로젝트 분석보다 먼저)
  → 고정된 Linear GraphQL 도구
  → 조회 즉시 응답 / 변경 초안
  → 같은 스레드의 `실행` 한 번
```

### 12. 설정·제한된 도구·안전한 전송

- [x] `.env`의 PC별 `LINEAR_API_KEY`, URL, timeout 설정을 추가한다.
- [x] HTTP·timeout·잘못된 JSON·GraphQL `errors`를 안전한 `LinearApiError`로 변환한다.
- [x] 팀/이슈 조회, 이슈 생성, 제목·설명·상태 갱신만 고정 GraphQL 문서로 제공한다.
- [x] API key, Authorization 헤더, GraphQL 요청·응답 본문을 로그·Slack에 남기지 않는다.

### 13. Slack 라우팅과 변경 확인

- [x] Linear 요청은 코드 실행·파일 생성·프로젝트 분석과 분리해 라우팅한다.
- [x] 조회는 즉시 처리하고 생성·수정은 typed draft 미리보기 후 `실행`에서만 mutation 한다.
- [x] 보류 Linear 작업은 스레드별 15분 TTL·단일 소비이며 `취소`는 해당 작업만 폐기한다.
- [x] 코드 실행 workflow에 보류 작업이 없으면 `실행`/`취소`를 Linear workflow로 전달한다.

검증: fake HTTP transport로 HTTP/GraphQL 오류, 키 누출 방지, 고정 variables, 조회 라우팅,
미리보기·실행 한 번·취소·만료를 테스트한다.
